# <Project Name> — agent memory

<!--
  PROJECT-level memory. Unlike global memory, this can be verbose — it's the
  collective learning of every agent session in this repo. Build it over time:
  whenever an agent does something wrong, correct it and ask it to record the
  lesson here so the next session doesn't repeat the mistake.

  Place at the repo root and symlink so both agents share it:
      ln -s CLAUDE.md AGENTS.md     # (or the reverse)
  Then `git add` both — the symlink is tracked.
-->

## What this project is
<one-paragraph description: what it does, who it's for>

## Repo layout
- `src/…` — …
- `…`

## Terminology
- **<term>** — <meaning>

## How key components work
<the non-obvious architecture an agent needs to be productive>

## How to run / test end-to-end
<exact commands to build, run, and validate a change like a real user would>

## Conventions
- <coding/PR/commit conventions specific to this repo>

## Lessons (append as agents learn)
- <date> — <mistake observed> → <rule to prevent it>
